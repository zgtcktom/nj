import { tester, arange, array, asarray, ones, zeros, slice, NDArray } from './core.mjs';

export function interp(x, xp, fp, left = null, right = null, period = null) {
	// not going to strict check
}

tester
	.add(
		tester,
		() => 0,
		() => 1
	)
	.add(
		tester,
		() => 0,
		() => 1
	)
	.add(
		tester,
		() => 0,
		() => 1
	);
